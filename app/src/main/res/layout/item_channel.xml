<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/channelItemRoot"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="75dp"
    android:background="@drawable/focus_selector"
    android:clickable="true"
    android:focusable="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="72dp"
        android:layout_alignParentTop="true"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingHorizontal="12dp">

        <CheckBox
            android:id="@+id/cbBulkSelect"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="8dp"
            android:clickable="false"
            android:focusable="false"
            android:visibility="gone" />

        <ImageView
            android:id="@+id/ivChannelLogo"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginEnd="12dp"
            android:background="#222222"
            android:padding="4dp"
            android:scaleType="fitCenter" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <!-- Quality tag parsed from the channel name itself (e.g. "ESPN HD" -> "HD") —
                     see ChannelQualityTag kdoc. Not a verified stream measurement, just labels
                     what the provider already claims, mainly to tell apart duplicate channels
                     from different providers. -->
                <TextView
                    android:id="@+id/tvQualityBadge"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginEnd="6dp"
                    android:background="@drawable/quality_badge_bg"
                    android:paddingHorizontal="4dp"
                    android:paddingVertical="1dp"
                    android:textColor="#FFFFFF"
                    android:textSize="9sp"
                    android:textStyle="bold"
                    android:visibility="gone" />

                <TextView
                    android:id="@+id/tvChannelName"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:ellipsize="end"
                    android:maxLines="1"
                    android:textColor="#FFFFFF"
                    android:textSize="14sp"
                    android:textStyle="bold" />
            </LinearLayout>

            <TextView
                android:id="@+id/tvEpgNow"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                android:textColor="#999999"
                android:textSize="12sp" />

            <TextView
                android:id="@+id/tvEpgNext"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                android:textColor="#666666"
                android:textSize="11sp"
                android:visibility="gone" />

            <!-- Which provider this channel came from (when it isn't the primary server) and its
                 category, e.g. "LEPTIS · SPORTS" — same gray-uppercase-label style as
                 item_merged_channel's tvServerNickname / the server tags in Settings > Servers.
                 Text is built as "$nickname · $category" (or just one half if the other is
                 missing) rather than a second TextView, to avoid a layout/row-height change
                 across every adapter that binds this view. -->
            <TextView
                android:id="@+id/tvServerNickname"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                android:textAllCaps="true"
                android:textColor="#777777"
                android:textSize="10sp"
                android:visibility="gone" />
        </LinearLayout>

        <View
            android:id="@+id/viewHealthDot"
            android:layout_width="8dp"
            android:layout_height="8dp"
            android:layout_marginStart="8dp"
            android:background="@drawable/health_dot"
            android:visibility="gone" />

        <ImageView
            android:id="@+id/ivFavorite"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginStart="8dp"
            android:padding="4dp"
            android:background="@drawable/focus_selector"
            android:src="@android:drawable/btn_star_big_off" />

        <ImageView
            android:id="@+id/ivDragHandle"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_marginStart="8dp"
            android:src="@android:drawable/ic_menu_sort_by_size"
            android:tint="#666666"
            android:visibility="gone" />

    </LinearLayout>

    <ProgressBar
        android:id="@+id/epgProgressBar"
        style="@android:style/Widget.ProgressBar.Horizontal"
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:layout_alignParentBottom="true"
        android:max="100"
        android:progress="0"
        android:progressBackgroundTint="#1A1A1A"
        android:progressTint="#008CFF"
        android:visibility="invisible" />

</RelativeLayout>
