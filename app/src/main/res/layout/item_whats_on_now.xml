<?xml version="1.0" encoding="utf-8"?>
<!-- One card in the "What's On Now" strip (see HomeActivity.updateWhatsOnNowStrip) — channel
     name, its current program title, and a progress bar showing how far into that program the
     live broadcast is. Tapping plays the channel directly. -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="150dp"
    android:layout_height="wrap_content"
    android:layout_marginEnd="8dp"
    android:background="@drawable/focus_selector"
    android:orientation="vertical"
    android:paddingHorizontal="10dp"
    android:paddingVertical="8dp">

    <TextView
        android:id="@+id/tvWhatsOnChannelName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="#008CFF"
        android:textSize="11sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/tvWhatsOnProgramTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:ellipsize="end"
        android:maxLines="2"
        android:minLines="2"
        android:textColor="#FFFFFF"
        android:textSize="12sp" />

    <ProgressBar
        android:id="@+id/whatsOnProgress"
        style="@android:style/Widget.ProgressBar.Horizontal"
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:layout_marginTop="6dp"
        android:max="100"
        android:progress="0"
        android:progressBackgroundTint="#33FFFFFF"
        android:progressTint="#008CFF" />

</LinearLayout>
